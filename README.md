# JavaFX Slide Show Application

> **JavaFX Slide Show** — десктопное приложение для создания, просмотра и управления слайдами.  
> Поддерживает загрузку изображений, заметки, индивидуальные анимации на слайд, музыкальное сопровождение и экспорт проекта в файл формата `.myshow`.

---

## 📌 Возможности

✔ Загрузка изображений из файлов с фильтром форматов  
✔ Добавление заметок к каждому слайду  
✔ Индивидуальные параметры анимации для каждого слайда  
✔ Проигрывание слайдшоу с заданной скоростью и музыкой  
✔ Drag-and-drop изменение порядка слайдов  
✔ Сохранение проекта в собственный формат (`.myshow`)  
✔ Загрузка проекта с восстановлением всех параметров  
✔ Удаление выбранного слайда  
✔ Сохранение слайда как PNG

---

## 🧠 Архитектура проекта

Проект построен с использованием следующих объектно-ориентированных паттернов:

### **Iterator**
Iterator используется для безопасного последовательного обхода набора слайдов без прямого доступа к структуре внутреннего хранилища. Это реализовано через класс `SlideIterator`, который работает с `SlideRepository`.

### **Builder**
Паттерн Builder используется для формирование индикации текущего состояния просмотра (например, *5 из 12*), обеспечивая гибкое построение строкового представления.

### **Абстрактная фабрика**
SlideFactory инкапсулирует создание слайд-агрегатов (`SlideAggregate`), позволяя отделить процесс создания объектов слайдов от их использования.

---

## 📁 Основные компоненты

### 🧩 Модель данных  
- `SlideAggregate` — агрегат, представляющий слайд (изображение, заметки, анимация).  
- `AnimationType` — перечисление типов анимации (Fade, Slide, Zoom).  

### 🏭 Фабрика  
- `SlideFactory` / `DefaultSlideFactory` — интерфейс и реализация для создания слайдов.

### 📦 Репозиторий и итерация  
- `SlideRepository` — хранит агрегаты слайдов.  
- `SlideIterator` — предоставляет итерационный доступ к слайдам.

### 💾 Сохранение / загрузка  
- `ProjectZipIO` — упаковка проекта в ZIP (`.myshow`) и восстановление слайдов, заметок, анимаций и музыки.

---

## 🚀 Быстрый старт

### 🎯 Требования

* Java 17+ или выше  
* JavaFX SDK (включая модуль `javafx.media`)  
* IDE (например IntelliJ IDEA)

---

### 📦 Установка

1. Клонировать репозиторий
   ```bash
   git clone https://github.com/yourusername/yourproject.git
